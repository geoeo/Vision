use nalgebra as na;

use na::{Matrix3};
use crate::Float;
use core::panic;



/**
    Rotation Coordiante Descent Parra et al.
 */
pub fn rcd(relative_rotations: &Vec<Vec<((usize, usize), Matrix3<Float>)>>) -> Vec<Vec<((usize, usize), Matrix3<Float>)>> {



    panic!("TODO");
}